/**
 * Note information, like title, last edit time etc
 */
export type NoteInfo = {
  title: string
  lastEditTime: number
}

/**
 * Represents the markdown content of the note itself
 */
export type NoteContent = string
